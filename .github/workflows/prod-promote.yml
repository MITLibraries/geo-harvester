name: Prod Image Promote
on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  id-token: write
  contents: read

jobs:
  promote:
    name: Prod promote
    uses: mitlibraries/.github/.github/workflows/ecr-multi-arch-promote-prod.yml@main
    secrets: inherit
    with:
      AWS_REGION: us-east-1
      GHA_ROLE_STAGE: geo-harvester-gha-stage
      GHA_ROLE_PROD: geo-harvester-gha-prod
      ECR_STAGE: geo-harvester-stage
      ECR_PROD: geo-harvester-prod
